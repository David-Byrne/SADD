[Unit]
Description=Classifies Tweets

[Service]
Type=simple
# TODO - come up with a way to remove 'david'
User=david
WorkingDirectory=/home/david/FYP/classifier
ExecStart=/usr/local/bin/gunicorn -w 4 server:app

[Install]
WantedBy=multi-user.target
